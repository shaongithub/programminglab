#include<stdio.h>
int main()
{
    int Num;

    scanf("%d", &Num);

    if(Num%2==0)
        printf("This is a Even Number\n");
    else
        printf("This is a Odd Number\n");

    return 0;
}
