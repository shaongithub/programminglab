#include<stdio.h>
int main()
{
    int A,B;
    scanf("%d %d", &A, &B);
    if(A>B)
        printf("Maximum Number is: %d\n", A);
    else
        printf("Maximum Number is: %d\n", B);
    return 0;
}
